# Example environment variables for the Google AI proxy
# Do NOT commit your real API key. Copy this file to `.env` and fill values locally.

# The API key for Google Cloud APIs (do NOT commit)
GOOGLE_AI_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# The target endpoint for your Google AI Studio / Generative API.
# Example (replace region/model as appropriate):
# https://generativelanguage.googleapis.com/v1beta2/models/text-bison-001:generateText
GOOGLE_AI_ENDPOINT=

# If your API requires a Bearer token instead of a key query param, set this to true.
GOOGLE_AI_USE_BEARER=false

# Port the local proxy will listen on
GOOGLE_AI_PROXY_PORT=3000
